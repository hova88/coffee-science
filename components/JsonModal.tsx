
// This component is no longer used in the Coffee Visualization flow.
// Returning empty component to satisfy file requirement if strictly needed, 
// but better to just leave it effectively empty/unused or minimal.

import React from 'react';
export const JsonModal: React.FC<any> = () => null;
